version: '3'

services:
    weather:
        build: .
        image: weather:local
        container_name: weather
        env_file:
            - config.env
        environment:
            - VIRTUAL_HOST=weather.jahiduls.io
        ports:
            - 8080:3000

